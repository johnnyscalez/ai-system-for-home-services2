import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Who's Your Best Tech? You Can't Actually Say — FieldBuilt AI",
  description: "Not your busiest — your best. See which tech closes the most and makes you the most per job. Free for 14 days, no card.",
}

export default function StartTechLayout({ children }: { children: React.ReactNode }) {
  return children
}
