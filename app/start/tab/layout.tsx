import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "You Were Right to Walk Away From ServiceTitan — FieldBuilt AI",
  description: "Your gut did the math right. Here's how to get everything you wanted from that platform — without the bill, the year, or the contract.",
}

export default function StartTabLayout({ children }: { children: React.ReactNode }) {
  return children
}
